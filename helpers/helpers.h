#ifndef HELPERS_H
#define HELPERS_H
#define NOT_FOUND_INDEX -1

void int_swap(int*, int, int);

void merge(int*, int, int, int);

int min(int*, int);

int max(int*, int);

void zeros(int*, int);

#endif